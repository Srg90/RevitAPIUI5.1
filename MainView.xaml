<Window x:Class="RevitAPIUI1.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RevitAPIUI1"
             mc:Ignorable="d" 
             MinHeight="150" MinWidth="300" MaxHeight="150" MaxWidth="300"
             Title="Идентификатор элемента">
    <StackPanel>
        <Button Content="Количество труб на активном виде" Command="{Binding SelectCommandPipe}" Margin="5"/>
        <Button Content="Вывести объем стен на активном виде" Command="{Binding SelectCommandWall}" Margin="5"/>
        <Button Content="Количество дверей на активном виде" Command="{Binding SelectCommandDoor}" Margin="5"/>

    </StackPanel>
</Window>
